// document.addEventListener('DOMContentLoaded', () => {
//     const loginForm = document.getElementById('login-form');
//     const errorMessage = document.getElementById('error-message');

//     loginForm.addEventListener('submit', (event) => {
//         event.preventDefault();

//         // Simulate admin ID and password validation (replace with actual validation logic)
//         const adminId = document.getElementById('admin-id').value;
//         const password = document.getElementById('password').value;

//         if (adminId === 'admin' && password === 'admin123') {
//             // Redirect to admin dashboard or perform any other action upon successful login
//             window.location.href = './admindashboard.html';
//         } else {
//             errorMessage.textContent = 'Invalid admin ID or password. Please try again.';
//         }
//     });
// });
